# Todo — Subventia Oracle MVP

**Date**: 2026-04-16  
**Branch**: `feature/subventia-oracle-mvp`  
**Architecture**: `tasks/architecture.md`  
**Stories**: `tasks/user-stories.md`

---

### Block 1 — Project Scaffolding

- [ ] T-1: Manually scaffold Next.js 14 project in the existing repo directory — create `package.json` (next, react, react-dom, tailwindcss, postcss, autoprefixer), `tailwind.config.js`, `postcss.config.js`, and `app/globals.css` with Tailwind directives (US-1)
- [ ] T-1b: `git checkout -b feature/subventia-oracle-mvp` — create feature branch before writing any implementation code (US-1)
- [ ] T-2: Install dependencies: `better-sqlite3`, `@anthropic-ai/sdk`, `lucide-react` (US-1)
- [ ] T-3: Write `next.config.js` with `serverExternalPackages: ['better-sqlite3']` (US-1)
- [ ] T-4: Create `.env.local` at project root — check if it already exists first; if not, ask the user for their `ANTHROPIC_API_KEY` value before writing the file (US-1)
- [ ] T-5: Confirm `.gitignore` excludes `.env.local` and `subventia.db`; add entries if missing (US-1)

> ⛔ **Context Checkpoint** — complete all tasks above, output the Handoff State (see CLAUDE.md), and wait for user "continue" before starting the next block.

---

### Block 2 — Database & API Route

- [ ] T-6: Write `lib/db.js` — singleton `getDb()`, `CREATE TABLE IF NOT EXISTS grants`, seed `mit`/`wbso`/`efro` rows idempotently (US-2)
- [ ] T-7: Write complete `app/api/analyze/route.js` — `async POST` handler: validate `proposal` (400 on missing), fetch grants synchronously from SQLite, embed `financial_analysis` skill prompt (pass proposal + serialised grants, instruct Claude to return only valid JSON ranked by `matchScore` desc), call Claude, strip markdown fences, `JSON.parse`, return ranked array; wrap Claude call + parse in try/catch returning `500 { error: "Analysis failed. Please try again." }` on any failure (US-3)
- [ ] T-8: Smoke-test the route — dev server running and `.env.local` must have a valid key; call `POST /api/analyze` with the sample proposal from `README.md` and confirm a ranked JSON array with all 3 grants is returned (US-3)

> ⛔ **Context Checkpoint** — complete all tasks above, output the Handoff State (see CLAUDE.md), and wait for user "continue" before starting the next block.

---

### Block 3 — Frontend

- [ ] T-11: Write `app/layout.jsx` — Stachanov Professional header (`bg-[#003366]`, "Subventia Oracle" title in white), page background `#F8FAFC`, import Tailwind globals (US-4)
- [ ] T-12: Write `app/page.jsx` — server component, renders `<ProposalForm />` centred below the header (US-4)
- [ ] T-13: Write `components/ProposalForm.jsx` — client component: full-width 8-row textarea, "Analyze" button with Lucide spinner while loading, `useState` for proposal/results/loading/error, calls `POST /api/analyze`, inline error message on failure (US-4)
- [ ] T-14: Write `components/GrantCard.jsx` — props: `grantName`, `matchScore`, `matchedCriterion`; renders score bar (width = `matchScore`%, colour by threshold), numeric score, one-sentence citation; card class: `rounded-lg border border-[#E2E8F0] bg-white shadow-sm` (US-4)
- [ ] T-15: Run `npm run dev`, open `localhost:3333`, paste the sample proposal from `README.md`, verify ranked cards render correctly end-to-end (US-4)
- [ ] T-16: Design-system compliance check — confirm no `rounded-xl`, no gradients, score bar colours match thresholds exactly, header hex is `#003366` (US-4)

> ⛔ **Context Checkpoint** — complete all tasks above, output the Handoff State (see CLAUDE.md), and wait for user "continue" before starting the next block.

---

### Block 4 — Git & GitHub

- [ ] T-17: `git status` — verify `.env.local` and `subventia.db` are NOT staged; stage all other source files (US-5)
- [ ] T-18: Commit with conventional message, e.g. `feat: add Subventia Oracle MVP — grant eligibility pre-screener` (US-5)
- [ ] T-19: Push feature branch `feature/subventia-oracle-mvp` to `origin` (US-5)
- [ ] T-20: Open a PR on `aukehofman97/stachanov-claude-workshop` using the `git-workflow` skill (US-5)

> ⛔ **Context Checkpoint** — complete all tasks above, output the Handoff State (see CLAUDE.md), and wait for user "continue" before starting the next block.
